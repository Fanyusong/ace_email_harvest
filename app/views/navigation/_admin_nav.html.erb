<div class="navbar navbar-default navbar-static-top">
  <div class="container">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="/admin">AceEmailHarvest</a>
    <div class="navbar-collapse collapse navbar-responsive-collapse">
      <ul class="nav navbar-nav">
        <% if current_admin %>
            <li><%= link_to "Manage user", admin_admin_index_path  %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_admin %>
            <li class="dropdown">
              <a class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                <%= full_name_of_user(current_admin) %>
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                <li><a href="<%= edit_admin_password_path %>"  class="dropdown-item"><i class="glyphicon glyphicon-user"></i> Edit Profile</a></li>
                <li><a href="<%= destroy_admin_session_path %>" data-method="delete"  class="dropdown-item"><i class="glyphicon glyphicon-off"></i> Logout</a></li>
              </ul>
            </li>
        <% else %>
            <li>
              <%= link_to('Sign In', new_admin_session_path, class: 'btn btn-primary', title: 'Sign In') %>
            </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
