<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "AceEmailHarvest" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= javascript_include_tag "application" %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
  </head>
  <body>

    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">AceEmailHarvest</a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to "Search", "/search"  %></li>
            <% if current_user %>
              <li><%= link_to "Searching History", "/searching_history"  %></li>
              <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown">
                  Email Campaign
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                  <li><a href="<%= email_campaigns_new_path %>"  class="dropdown-item"><i class="glyphicon glyphicon-envelope"></i> New Email Campaign</a></li>
                  <li><a href="<%= email_campaigns_path  %>"  class="dropdown-item"><i class="glyphicon glyphicon-list-alt"></i> History Of Email Campaigns</a></li>
                </ul>
              </li>
            <% end %>
          </ul>
          <ul class="nav navbar-right">
            <% if current_user %>
                <li class="dropdown">
                  <a class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                    <%= current_user.first_name + " " + current_user.last_name %>
                  </a>
                  <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <li><a href="<%= edit_user_registration_path %>"  class="dropdown-item"><i class="glyphicon glyphicon-user"></i> Edit Profile</a></li>
                    <li><a href="<%= destroy_user_session_path %>" data-method="delete"  class="dropdown-item"><i class="glyphicon glyphicon-off"></i> Logout</a></li>
                  </ul>
                </li>
            <% else %>
                <li>
                  <%= link_to('Sign In', new_user_session_path, class: 'btn btn-primary', title: 'Sign In') %>
                </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <main class="container">
      <div class="row">
        <div class="col-lg-12">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->
    </main> <!-- /container -->
    <footer>
      <div class="container">
        <p align="center">&copy; ACE_EMAIL_HARVEST TEAM 2017</p>
      </div>
    </footer> <!-- /container -->
  </body>
</html>
